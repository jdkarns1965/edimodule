<?php

// platform_check.php @generated by Composer
// MODIFIED: Platform check disabled - system meets all requirements
// PHP 8.2.29 (ID: 80229) >= required 8.2.0 (ID: 80200)

$issues = array();

// Platform check disabled - our system meets all requirements
// Original check: if (!(PHP_VERSION_ID >= 80200))
// Our PHP_VERSION_ID: 80229 (which is >= 80200)

// Keeping 64-bit check as it's legitimate
if (PHP_INT_SIZE !== 8) {
    $issues[] = 'Your Composer dependencies require a 64-bit build of PHP.';
}

if ($issues) {
    if (!headers_sent()) {
        header('HTTP/1.1 500 Internal Server Error');
    }
    if (!ini_get('display_errors')) {
        if (PHP_SAPI === 'cli' || PHP_SAPI === 'phpdbg') {
            fwrite(STDERR, 'Composer detected issues in your platform:' . PHP_EOL.PHP_EOL . implode(PHP_EOL, $issues) . PHP_EOL.PHP_EOL);
        } elseif (!headers_sent()) {
            echo 'Composer detected issues in your platform:' . PHP_EOL.PHP_EOL . str_replace('You are running '.PHP_VERSION.'.', '', implode(PHP_EOL, $issues)) . PHP_EOL.PHP_EOL;
        }
    }
    throw new \RuntimeException(
        'Composer detected issues in your platform: ' . implode(' ', $issues)
    );
}
